[flake8]
ignore = C101,E251,E128,E501
max-line-length = 119
exclude =
    *.egg-info,
    .eggs,
    .git,
    .settings,
    .tox,
    .env,
    .venv,
    build,
    data,
    dist,
    docs,
    private,
    *migrations*,
    node_modules,
    requirements,
    tmp,
    *node_modules*

[isort]
line_length = 119
skip = manage.py, *migrations*, node_modules, .tox, .eggs, data, .env, .venv, private
include_trailing_comma = true
multi_line_output = 5
not_skip = __init__.py
lines_after_imports = 2
default_section = THIRDPARTY
sections = FUTURE, STDLIB, DJANGO, CMS, THIRDPARTY, FIRSTPARTY, LIB, LOCALFOLDER
known_first_party = djangocms_text_ckeditor
known_cms = cms, menus
known_django = django
